name: app_platform_network
description: Standardized HTTP networking layer with token injection, request/response handling, and typed parsing.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  http: ^0.13.0
  app_platform_core:
    path: ../core
