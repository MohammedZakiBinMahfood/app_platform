name: app_platform_ui
description: Reusable UI widgets including AsyncView, loading indicators, error views, and form fields.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  app_platform_core:
    path: ../core
